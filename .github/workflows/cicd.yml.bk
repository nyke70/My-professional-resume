name: My first pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Clone the code
        uses: actions/checkout@v4
      - name: Build image
        run: 
          docker build -t nyke70/cicd-portfolio .
      - name: Docker login
        uses: docker/login-action@v3
        with:
          username: nyke70
          password: ${{ secrets.DOCKER_PASSWD }}
      - name: Push image
        run: 
          docker push nyke70/cicd-portfolio                  
 


